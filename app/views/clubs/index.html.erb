<div class="container">
	<h2>The club</h2>
	<!-- Pas nécessaire de gérer le cas où le club est vide puisque si une personne voit cette page, ça signifie qu'elle est connectée et qu'il y a au moins elle dans le club -->
	<p>Let's see our different profil in the club :</p>
	<table class="table">
  		<thead>
	    <tr>
	      <th scope="col">First Name</th>
	      <th scope="col">Last Name</th>
	      <th scope="col">Email</th>
	    </tr>
	  </thead>
	  <tbody>
	  	<% User.all.each do |user| %>
	    	<tr>
	     		<td><%= user.first_name %></td>
	      		<td><%= user.last_name %></td>
	      		<td><%= user.email %></td>
	    	</tr>
	   	<% end %>
	  </tbody>
	</table>
</div>